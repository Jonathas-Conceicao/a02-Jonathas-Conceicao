#include <stdlib.h>
#include "memvirt.h"

struct result * memvirt(int num_procs, uint32_t num_frames, char * filename, uint32_t interval) {
  (void)num_procs;
  (void)num_frames;
  (void)filename;
  (void)interval;

  return NULL;
}
